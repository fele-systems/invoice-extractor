com.fele.integration.test=true

server.port=33531
spring.thymeleaf.cache= false
spring.servlet.multipart.max-file-size=-1
spring.servlet.multipart.max-request-size=-1

# Integration tests database
spring.datasource.url=jdbc:postgresql:invoices_integration
spring.datasource.username=invoices
spring.datasource.password=invoice
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.initial-script=schema-test.sql

# Unit tests database
# jdbc.driverClassName=org.h2.Driver
# jdbc.url=jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE;MODE=PostgreSQL

# hibernate.dialect=org.hibernate.dialect.H2Dialect
# hibernate.hbm2ddl.auto=create

logging.level.org.springframework.security.web.FilterChainProxy=DEBUG
logging.level.org.springframework.security=DEBUG
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true